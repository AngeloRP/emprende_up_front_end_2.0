import { TableRewatcherDirective } from './table-rewatcher.directive';

describe('TableRewatcherDirective', () => {
  it('should create an instance', () => {
    const directive = new TableRewatcherDirective();
    expect(directive).toBeTruthy();
  });
});
